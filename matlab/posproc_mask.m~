function mask_final = posproc_mask(mask)

dims = [2 3 4];
se = strel('disk',2);

mask = imdilate(mask,se);
for d=1:dims
   se2 = strel('disk',d);
   mask = imclose(imopen(mask,se2));
end

mask_final = mask;
